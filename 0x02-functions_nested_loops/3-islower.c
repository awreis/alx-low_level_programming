#include "main.h"
#include <ctype.h>

/**
 * int _islower - checks for lower case
 *
 * Return: Return 1 if c is lower, otherwise 0
 */

int _islower()
{
int c;
if (_islower(c))
{
return (1);
}
else
{
return (0);
}
}
