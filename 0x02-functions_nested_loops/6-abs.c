#include "main.h"

/**
 * _abs - computes value of an integer x
 * @x - declared parameter
 *
 * Return: returns 0
 */

int _abs(int x)
{
if (x > 0)
{
return (x);
}
else if (x < 0)
{
return (x);
}
return (0);
}
