#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies integers
 * @a: integer one
 * @b: integer two
 * Return: result
 */

int mul(int a, int b)
{
	int result;
	result = a * b;
	return (result);
}
