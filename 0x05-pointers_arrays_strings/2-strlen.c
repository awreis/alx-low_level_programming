#include "main.h"
#include <stdio.h>

/**
 * _strlen - This fumction returns the length
 * of a string
 * @s: integer parameter
 * Return: s
 */

int _strlen(char *s)
{
	int len;

	len = _strlen(s);
	return (len);
}
