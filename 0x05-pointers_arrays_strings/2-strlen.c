#include "main.h"
#include <string.h>
#include <stdio.h>

/**
 * _strlen - This fumction returns the length
 * of a string
 * @s: integer parameter
 * Return: s
 */

int _strlen(char *s)
{
	char x;
	int length;

	length = _strlen(x);

	return (length);
}
