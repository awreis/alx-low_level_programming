#include "main.h"
#include <string.h>

/**
 * _strlen - This fumction returns the length
 * of a string
 * @s: integer parameter
 * Return: s
 */

int _strlen(char *s)
{
	int length;

	length = _strlen(*s);

	return (length);
}
